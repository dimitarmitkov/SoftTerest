{{> header}}
<div class="container home some">
    <img class="det-img" src="{{imageURL}}" />
    <div class="desc">
        <h2 class="display-5">{{title}}</h2>
        <p class="infoType">Description:</p>
        <p class="idea-description">{{description}}
        </p>
        <p class="infoType">Likes: <large>{{likes}}</large>
        </p>
        <p class="infoType">Comments:</p>
        <ul>
            {{#each donors}}
            <li class="comment">{{this}}</li>
            {{else}}
            <li class="comment">No comments yet :(</li>
            {{/each}}
        </ul>
    </div>
    {{#if canDonate}}
    <form class="text-center" method="POST" action="#/cause/donate/{{id}}">
        <textarea class="textarea-det" name="newComment" id=""></textarea>
        <button type="submit" class="btn detb" >Comment</button>
        <a class="btn detb" href="#/cause/like/{{id}}">Like</a>
    </form>
    {{else}}
    <div class="text-center">
        <a class="btn detb" href="#/cause/close/{{id}}">Delete</a>
    </div>
    {{/if}}
</div>
{{> footer}}

<!--    <img class="det-img" src="./images/dinner.jpg" />-->